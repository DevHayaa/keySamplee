import BookingForm from '@/components/BookingForm';

export default function Home() {
  return (
    <main className="min-h-screen bg-gradient-to-r from-blue-100 to-yellow-100 flex justify-center items-center">
      <BookingForm />
    </main>
  );
}
